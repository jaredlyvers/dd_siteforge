<div class="dd-banner {{parent_class}}" data-sal="{{sal}}" style="background-image: url({{parent_image_url}});">
  <div class="dd-banner__image">
    <picture>
      <img src="{{parent_image_url}}" class="dd-img" alt="{{parent_image_alt}}" />
    </picture>
  </div>
</div>
